{
  "name": "iccl-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website for Inhale Culture Celebrate Life Ltd — built with Eleventy + esbuild, deployed to GitHub Pages.",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\"",
    "build:ts": "esbuild src/ts/nav.ts --bundle --target=es2020 --outfile=_site/nav.js --minify",
    "build:html": "eleventy",
    "build:assets": "node -e \"const fs=require('fs');fs.mkdirSync('_site',{recursive:true});fs.copyFileSync('Constitution of Inhale Culture Celebrate Life Ltd (ICCL) - 2025.pdf','_site/Constitution of Inhale Culture Celebrate Life Ltd (ICCL) - 2025.pdf');fs.writeFileSync('_site/.nojekyll','')\"",
    "build": "npm run clean && npm run build:ts && npm run build:html && npm run build:assets",
    "check:site": "node scripts/check-built-site.mjs",
    "verify": "npm run typecheck && npm run build && npm run check:site",
    "dev:ts": "esbuild src/ts/nav.ts --bundle --target=es2020 --outfile=_site/nav.js --watch",
    "dev:html": "eleventy --serve",
    "dev": "npm-run-all --parallel dev:ts dev:html",
    "preview": "npm run build && node scripts/preview-built-site.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.4",
    "esbuild": "^0.24.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.0"
  }
}
